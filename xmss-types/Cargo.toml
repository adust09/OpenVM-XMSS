[package]
name = "xmss-types"
version = "0.1.0"
edition = "2021"
rust-version = "1.87"

[dependencies]
serde = { workspace = true, default-features = false, features = ["derive"] }

[features]
default = ["std"]
std = ["serde/std"]

[dev-dependencies]
serde_json = "1.0"
